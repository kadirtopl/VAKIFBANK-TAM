<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ShopingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/grey</item>
        <item name="colorOnPrimary">@android:color/white</item> <!-- Üstte gözüken metin rengi -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- Durum çubuğu rengi -->
        <item name="android:windowLightStatusBar">true</item> <!-- Beyaz metin için -->
    </style>

    <style name="Theme.ShopingApp" parent="Base.Theme.ShopingApp" />
</resources>
