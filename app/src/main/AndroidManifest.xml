<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopingApp"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activity.RecommendDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activity.IntroActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.BasicActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.SignupActvitiy"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.activity.MainActivity"
            android:exported="false" />
    </application>

</manifest>